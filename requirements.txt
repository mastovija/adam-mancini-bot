# ─────────────────────────────────────────────────────────────
# Adam Mancini Bot - Python dependencies
# ─────────────────────────────────────────────────────────────

# ── Scraping ──────────────────────────────────────────────────
# twikit removed: replaced by Playwright (playwright_utils.py + twitter_scraper_playwright.py)
requests>=2.31.0       # HTTP requests for Substack and APIs
beautifulsoup4>=4.12.0 # Parse the Substack newsletter HTML
lxml>=5.0.0            # Fast HTML parser for beautifulsoup

# ── LLM / Artificial Intelligence ─────────────────────────────
anthropic>=0.25.0      # Claude API — we use Haiku (~$3-5/month)

# ── Vector store (RAG) ────────────────────────────────────────
chromadb>=0.5.0        # Local vector database, free

# ── Market data ───────────────────────────────────────────────
ib_insync>=0.9.86      # IBKR ES Futures — primary source (DATA_SOURCE='ibkr' in config.py)
                       # Requires IB Gateway running on Mac, port 4002 (paper trading)
                       # After installing IB Gateway: enable API in settings → port 4002
alpaca-py>=0.25.0      # Alpaca SPY — fallback if IBKR Gateway is not available

# ── Telegram Bot ──────────────────────────────────────────────
python-telegram-bot>=21.0  # Send alerts to the phone

# ── Data and technical analysis ───────────────────────────────
pandas>=2.0.0          # Market data manipulation
pandas-ta>=0.3.14b     # Technical indicators (RSI, VWAP, etc.)
numpy>=1.24.0          # Numerical computations

# ── Utilities ─────────────────────────────────────────────────
python-dotenv>=1.0.0   # Load variables from .env
apscheduler>=3.10.0    # Scheduled tasks (daily parsing, monitoring)
pytz>=2024.1           # Time zones (NY market = EST)

playwright>=1.40.0     # Tweet monitor (intercepts X GraphQL)
# After installing: playwright install chromium